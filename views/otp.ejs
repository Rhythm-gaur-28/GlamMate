<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>OTP Verification | GlamMate</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(to right, #f9d5ec, #fbe8e5);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .otp-box {
      background-color: white;
      padding: 2rem 2.5rem;
      border-radius: 15px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
      text-align: center;
    }

    .otp-box h2 {
      margin-bottom: 1rem;
      color: #512da8;
    }

    input[type="text"] {
      padding: 10px;
      font-size: 16px;
      width: 100%;
      margin: 15px 0;
      border: 1px solid #ccc;
      border-radius: 8px;
    }

    button {
      background-color: #512da8;
      color: white;
      padding: 10px 25px;
      border: none;
      border-radius: 8px;
      font-weight: 600;
      cursor: pointer;
    }

    .back {
      margin-top: 1rem;
      font-size: 14px;
    }

    .back a {
      color: #512da8;
      text-decoration: none;
    }
  </style>
</head>
<body>

  <div class="otp-box">
    <h2>Enter Your OTP</h2>
    <p>We’ve sent a 6-digit code to <strong><%= email %></strong></p>
    <form action="/verify" method="POST">
      <input type="text" name="otp" placeholder="Enter OTP here" required />
      <button type="submit">Verify</button>
    </form>
    <div class="back">
      <a href="/auth">← Go back to Auth Page</a>
    </div>
  </div>

</body>
</html>
